# Frontend Environment Variables
# Copy this file to .env and replace with your actual values

# API Base URL for frontend to communicate with backend
VITE_API_BASE_URL=http://localhost:3001/api

# OpenAI API Key (for frontend direct calls if needed)
VITE_OPENAI_API_KEY=your_openai_api_key_here

# OCR API Key (for frontend direct calls if needed)
VITE_OCR_API_KEY=your_ocr_api_key_here

# Azure OpenAI Configuration (optional)
VITE_AZURE_OPENAI_ENDPOINT=your_azure_endpoint_here
VITE_AZURE_OPENAI_API_KEY=your_azure_api_key_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace placeholder values with your actual API keys
# 3. The VITE_ prefix is required for Vite to expose variables to the frontend
# 4. Never commit the .env file with real API keys to version control

# Note: Most API calls should go through the backend server for security
# Frontend environment variables are exposed to the client-side code
